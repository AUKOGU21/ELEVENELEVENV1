Brand Library logos.

Drop each brand logo here as a PNG, named by its slug:
  tibi.png
  negative-underwear.png
  jaded-london.png

Transparent background works best. Any square-ish size is fine (they render inside a rounded box). Once committed + deployed, they replace the text wordmarks automatically.
